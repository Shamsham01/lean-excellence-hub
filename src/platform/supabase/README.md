# Generated database types

Run `npm run db:start` followed by `npm run db:types` to generate
`database.types.ts` from the local database. The generated file is intentionally
untracked until an approved milestone introduces application schema.
