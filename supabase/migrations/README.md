# Database migrations

Milestone 2 establishes this migration directory without introducing domain
schema. Create future migrations with `npm exec supabase migration new <name>`
only after the relevant milestone is approved.
